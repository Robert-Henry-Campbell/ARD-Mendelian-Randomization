% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panukb_snp_grabber.R
\name{panukb_snp_grabber}
\alias{panukb_snp_grabber}
\title{Fetch outcome SNP rows from Pan-UKB sumstats for each outcome (R-based Tabix)}
\usage{
panukb_snp_grabber(
  exposure_snps,
  MR_df,
  ancestry,
  cache_dir = ardmr_cache_dir(),
  verbose = TRUE,
  force_refresh = FALSE
)
}
\arguments{
\item{exposure_snps}{mapped SNPs (must include \code{panukb_chrom} and \code{panukb_pos})}

\item{MR_df}{tibble with provider info (must contain 'aws_link' and 'aws_link_tabix')}

\item{ancestry}{character (AFR, AMR, CSA, EAS, EUR, MID)}

\item{cache_dir}{(unused here; kept for parity)}

\item{verbose}{logical}
}
\value{
MR_df with a new list-column \code{outcome_snps} (TwoSampleMR-formatted)
}
\description{
Uses Rsamtools/Rhtslib to remotely query tabix-indexed BGZF files over HTTP(S),
removing the need for a system 'tabix' in PATH. Standardizes output with
TwoSampleMR::format_data(type = "outcome").
}
