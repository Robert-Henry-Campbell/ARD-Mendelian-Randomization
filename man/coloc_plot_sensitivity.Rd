% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coloc_plot.R
\name{coloc_plot_sensitivity}
\alias{coloc_plot_sensitivity}
\title{Render coloc::sensitivity() with proper trait names and wider margins}
\usage{
coloc_plot_sensitivity(
  abf_res,
  basepath,
  exp_label = NULL,
  out_label = NULL,
  wrap = 30,
  rule = "H4 > 0.8",
  w_in = 7.2,
  h_in = 5.4,
  left_mar = 4.4,
  xlab_p12 = "Shared causal SNP prior (p12)",
  manh_label_line = 1.7
)
}
\arguments{
\item{abf_res}{Output of \code{coloc::coloc.abf()}.}

\item{basepath}{File-path stem (no extension); \code{.png} and \code{.pdf} are
written.}

\item{exp_label, out_label}{Character. Names of the exposure and outcome
traits. Long values are wrapped at \code{wrap} characters across multiple
lines. NULL leaves the default \code{"trait 1"} / \code{"trait 2"}.}

\item{wrap}{Integer. Wrap width in characters for trait labels.}

\item{rule}{Sensitivity rule passed straight to \code{coloc::sensitivity()}.}

\item{w_in, h_in}{Plot dimensions in inches.}

\item{left_mar}{Substituted \code{par(mar)[2]} value (in lines) to give the
prior/posterior y-axis labels breathing room.}

\item{xlab_p12}{Replacement label for the prior/posterior panels' x-axis
(coloc hardcodes \code{"p12"}). Pass any string or \code{expression()}. Defaults
to a self-explanatory phrase.}

\item{manh_label_line}{Distance (in \code{mex} lines) of the \strong{manhattan
panels'} axis labels from their axes. Coloc's default \code{mgp[1]} is
\code{2}. Lower values (e.g. \code{1.7}) bring "Chromosome position" and
"-log10(p)" closer to their axes without affecting the prior/
posterior probability panels.}
}
\value{
Invisibly returns the \code{basepath} stem.
}
\description{
Wraps \code{coloc::sensitivity()} so that:
\itemize{
\item the \code{"trait 1"} / \code{"trait 2"} panel titles are replaced with the actual
exposure / outcome labels (text-wrapped at \code{wrap} characters), and
\item the y-axis labels in the prior/posterior probability panels stop
overlapping the legend (achieved with a wider PNG/PDF and a slightly
larger left margin).
}
}
\details{
Implementation note: the installed \code{coloc} (v5.x) hardcodes the panel
titles and the \code{par(mar = c(3, 3, 2, 1))} left-margin via base graphics.
To override without forking the package we briefly \code{assignInNamespace()}
a wrapper around \code{graphics::title} (translating \code{"trait 1"} /
\code{"trait 2"} to the user's labels) and \code{graphics::par} (substituting a
larger left margin only when the exact \code{mar = c(3, 3, 2, 1)} call comes
through). Both are restored on exit.
}
\keyword{internal}
