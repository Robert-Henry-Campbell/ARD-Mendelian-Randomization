% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment_business_logic.R
\name{beta_contrast_by_cause}
\alias{beta_contrast_by_cause}
\title{Inside-vs-outside contrast of mean MR betas by cause}
\usage{
beta_contrast_by_cause(
  results_df,
  level = c("cause_level_1", "cause_level_2", "cause_level_3"),
  use_qc_pass = TRUE,
  min_nsnp = 2,
  exposure = NULL,
  Multiple_testing_correction = c("BH", "bonferroni"),
  alpha = 0.05
)
}
\arguments{
\item{results_df}{Tidy MR results with columns: results_beta_ivw, results_se_ivw,
results_qc_pass (optional), cause_level_* (chosen via \code{level}).}

\item{level}{"cause_level_1","cause_level_2","cause_level_3".}

\item{use_qc_pass}{If TRUE, keep results_qc_pass==TRUE when present.}

\item{min_nsnp}{Optional integer filter on results_nsnp_after.}

\item{exposure}{Optional label for reporting.}

\item{Multiple_testing_correction}{"BH" or "bonferroni" for q-values.}

\item{alpha}{Significance level (default 0.05).}
}
\value{
Tibble with one row per cause: n_in, n_out, beta_in, beta_out,
delta_beta, se_delta, z, p, q, ci_low, ci_high.
}
\description{
For each cause (at a chosen level), compute inverse-variance weighted means
of beta_IVW inside the cause and outside the cause, then the difference
Δβ = β̄_in - β̄_out, its SE, z, p, and BH/Bonferroni q across causes.
}
\details{
Assumes all betas are on a common scale (e.g., log-OR per 1 SD exposure).
}
