% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld_matrix.R
\name{compute_ld_matrix}
\alias{compute_ld_matrix}
\title{Compute a pairwise LD (signed r) matrix between exposure SNPs}
\usage{
compute_ld_matrix(
  exposure_snps,
  ancestry,
  cache_dir = ardmr_cache_dir(),
  out_csv = NULL,
  confirm = c("ask", "yes", "no"),
  force_refresh = FALSE,
  verbose = TRUE,
  plink_bin = NULL,
  ld_ref_dir = NULL,
  include_dropped = TRUE
)
}
\arguments{
\item{exposure_snps}{Data frame of exposure instruments. Must contain
\code{SNP}, \code{effect_allele.exposure}, \code{other_allele.exposure} (validated via
\code{\link[=assert_exposure]{assert_exposure()}}).}

\item{ancestry}{Character ancestry label (mapped to a 1000G superpop via
\code{\link[=ld_pop_from_ancestry]{ld_pop_from_ancestry()}}).}

\item{cache_dir}{Cache root (default \code{\link[=ardmr_cache_dir]{ardmr_cache_dir()}}). RDS caches go to
\verb{<cache_dir>/ld/}; the reference panel goes to \verb{<ld_ref_dir>} (default
\verb{<cache_dir>/ld_reference}).}

\item{out_csv}{Optional path for the CSV output. If \code{NULL}, no CSV is
written and only the cache + return value are produced.}

\item{confirm}{One of \code{"ask"}, \code{"yes"}, \code{"no"} (passed to
\code{\link[=ld_reference_checker]{ld_reference_checker()}} when the panel is missing).}

\item{force_refresh}{Logical; if \code{TRUE}, ignore any RDS cache hit and
recompute (the reference panel is left in place).}

\item{verbose}{Logical.}

\item{plink_bin}{Optional explicit path to the PLINK binary. If \code{NULL},
resolution order is \code{ARDMR_PLINK_BIN} env var,
\code{genetics.binaRies::get_plink_binary()}, then \code{Sys.which("plink"|"plink2")}.}

\item{ld_ref_dir}{Optional directory holding the reference panel. Defaults
to \code{file.path(cache_dir, "ld_reference")}.}

\item{include_dropped}{Logical; if \code{TRUE} (default), SNPs not represented
in the panel (or with allele mismatches) appear as NA rows/columns in
the CSV so the dimensionality matches the input. If \code{FALSE}, they are
omitted.}
}
\value{
Invisibly, a list with \code{matrix} (numeric matrix with SNP
row/colnames), \code{snps_used}, \code{snps_dropped} (data frame of \verb{SNP, reason}),
\code{ld_pop}, \code{csv_path}, \code{cache_path}.
}
\description{
Wraps PLINK 1.9/2.0 to compute the signed r LD matrix between the SNPs in
\code{exposure_snps}, using the MRC-IEU 1000G reference panel for the
superpopulation derived from \code{ancestry}. Results are cached on disk by
(sorted SNPs + population + panel version), and optionally written as a
CSV at \code{out_csv}. Sign is harmonised so that a positive entry corresponds
to the exposure effect alleles being correlated in the reference panel.
}
\details{
Note: \code{force_refresh = TRUE} invalidates the LD-matrix RDS cache but does
\strong{not} redownload the (large, stable) reference panel.
}
