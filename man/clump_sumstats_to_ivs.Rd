% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coloc_clump.R
\name{clump_sumstats_to_ivs}
\alias{clump_sumstats_to_ivs}
\title{Derive TwoSampleMR-formatted exposure IVs from a GWAS-VCF.}
\usage{
clump_sumstats_to_ivs(
  vcf_path,
  ancestry,
  p_threshold = 5e-08,
  r2 = 0.001,
  kb = 10000L,
  f_threshold = 10,
  genome_build = "GRCh37",
  cache_dir = ardmr_cache_dir(),
  clump_opts = list(),
  confirm = "ask",
  verbose = TRUE
)
}
\arguments{
\item{vcf_path}{Path to a tabix-indexed GWAS-VCF (MRC IEU spec).}

\item{ancestry}{Ancestry code (used to derive the LD population).}

\item{p_threshold, r2, kb, f_threshold}{Legacy scalar args kept for
backward compat. When \code{clump_opts} is supplied, its values override
these scalars.}

\item{genome_build}{Genome build of the VCF (only "GRCh37" supported).}

\item{cache_dir}{Cache root.}

\item{clump_opts}{Named list overriding the scalar args; same fields as
documented at \code{?run_phenome_mr} (\code{p_backoff}, \code{r2}, \code{kb},
\code{f_threshold}, \code{maf_min}, \code{info_min}, \code{drop_indels},
\code{drop_palindromic}).}

\item{confirm}{One of \code{"ask"}, \code{"yes"}, \code{"no"}; passed to
\code{\link[=ld_reference_checker]{ld_reference_checker()}} when the LD panel must be installed for
clumping.}

\item{verbose}{Logical.}
}
\description{
Queries the VCF for hits at the loosest p-value rung in \code{p_backoff},
converts to TwoSampleMR shape, and routes through
\code{\link[=preprocess_exposure_snps]{preprocess_exposure_snps()}} for the unified pipeline (p-value backoff,
LD clumping, rsid-validation/dedupe, indel/palindromic, F-stat, MAF,
INFO). The clumping always runs locally against the 1kg.v3 panel;
\code{already_clumped} and \code{already_p_filtered} are forced to FALSE because
VCF-derived IVs come straight from the file with no upstream filtering.
}
