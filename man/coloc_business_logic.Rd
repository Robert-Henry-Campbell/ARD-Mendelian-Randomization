% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coloc_business_logic.R
\name{coloc_business_logic}
\alias{coloc_business_logic}
\title{Run coloc.abf (+ coloc.susie when LD computable) per IV-region.}
\usage{
coloc_business_logic(
  hdat_use,
  exposure_snps,
  exposure_region_fetcher,
  exposure_metadata,
  ancestry,
  outcome_region_fetcher,
  outcome_metadata,
  window_kb = 500L,
  plot_dir = "",
  cache_dir = ardmr_cache_dir(),
  priors = list(p1 = 1e-04, p2 = 1e-04, p12 = 1e-05),
  skip_mhc = TRUE,
  susie_always = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{hdat_use}{Harmonised exposure-outcome tibble (post-\code{harmonise_data},
\code{mr_keep} filtered).}

\item{exposure_snps}{Original exposure SNPs tibble; used as a fallback for
IV positions if \code{hdat_use} lacks \code{chr.outcome}/\code{pos.outcome}.}

\item{exposure_region_fetcher}{\verb{function(chr, start, end)} returning a
tibble of exposure SNPs with \verb{chr, pos, effect_allele, other_allele, beta, se, eaf, pval}.}

\item{exposure_metadata}{\code{list(N, type, ncase, ncontrol, s)}.}

\item{ancestry}{Ancestry code (1000G + Pan-UKB style, e.g. \code{"EUR"}).}

\item{outcome_region_fetcher}{Same signature as \code{exposure_region_fetcher}.}

\item{outcome_metadata}{Same shape as \code{exposure_metadata}.}

\item{window_kb}{Half-window around each IV (default 500).}

\item{plot_dir}{Directory for per-locus artefacts (\code{""} = off).}

\item{cache_dir}{Cache directory.}

\item{priors}{Coloc priors as \code{list(p1, p2, p12)}.}

\item{skip_mhc}{Skip loci overlapping chr6:25Mb-35Mb (default \code{TRUE}).}

\item{susie_always}{Try coloc.susie whenever LD is computable, regardless
of IV count in the locus (default \code{TRUE}).}

\item{verbose}{Logical.}
}
\value{
Tibble (one row per locus) with \verb{locus_id, chr, start, end, n_ivs_in_locus, n_snps_used, pp_h4_abf, pp_h4_susie_max, method_passed, coloc_dir}.
}
\description{
Run coloc.abf (+ coloc.susie when LD computable) per IV-region.
}
