% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coloc_source_resolver.R
\name{.resolve_coloc_source}
\alias{.resolve_coloc_source}
\title{Resolve the (exposure_snps, exposure_sumstats, exposure_id) trio into the
inputs the rest of the pipeline expects: a tibble of IVs plus a coloc
fetcher closure and metadata list.}
\usage{
.resolve_coloc_source(
  exposure_snps = NULL,
  exposure_sumstats = NULL,
  exposure_id = NULL,
  ancestry,
  genome_build = "GRCh37",
  acknowledge_no_coloc = FALSE,
  clump_opts = list(),
  cache_dir = ardmr_cache_dir(),
  confirm = "ask",
  verbose = TRUE
)
}
\value{
list(mode, exposure_snps, coloc_fetcher, coloc_metadata,
clump_opts_resolved, preprocessing_steps).
\code{preprocessing_steps} is a list (may be empty when
\code{clump_opts$preprocess = FALSE} or for \code{vcf_only} whose
\code{clump_sumstats_to_ivs} runs the preprocessor internally and
returns just the SNPs).
}
\description{
Modes:
\itemize{
\item "snps_only"   : SNPs supplied, no coloc; requires acknowledge_no_coloc.
\item "snps_vcf"    : SNPs as IVs; VCF for coloc.
\item "vcf_only"    : Derive IVs by clumping VCF; VCF for coloc.
\item "ieugwasr"    : Extract IVs from OpenGWAS id; same id for coloc.
\item "snps_id"     : SNPs as IVs (already filtered) + id for coloc only.
Allowed but warns; primarily for ard_compare's
PheWAS-filtered flow.
}
}
\details{
All modes route their IV tibble through \code{\link[=preprocess_exposure_snps]{preprocess_exposure_snps()}} for
a single, unified exposure-SNP preprocessing pipeline. The \code{ieugwasr}
branch handles the p-value backoff ladder at the source-fetch layer
(fetching unclumped via \code{extract_instruments(clump = FALSE)} or
\code{tophits} per rung) and passes \verb{already_p_filtered = TRUE, already_clumped = FALSE} to the preprocessor. Local LD clumping then
runs against the ancestry-aware 1kg.v3 panel inside
\code{preprocess_exposure_snps}. The \verb{snps_*} modes leave both flags
FALSE so the preprocessor walks the ladder itself by re-filtering
the input tibble.
}
\keyword{internal}
