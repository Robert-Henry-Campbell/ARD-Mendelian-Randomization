% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coloc_data_fetch_neale.R
\name{.coloc_neale_chr_slice}
\alias{.coloc_neale_chr_slice}
\title{Read and cache a per-chromosome slice of a Neale .tsv.bgz file.}
\usage{
.coloc_neale_chr_slice(
  file_path,
  chr,
  cache_dir = ardmr_cache_dir(),
  verbose = TRUE,
  force_refresh = FALSE
)
}
\arguments{
\item{file_path}{Absolute path to a Neale \code{.tsv.bgz} file.}

\item{chr}{Chromosome to extract (character; e.g. \code{"6"} or \code{"X"}).}

\item{cache_dir}{Cache root.}

\item{verbose}{Logical.}

\item{force_refresh}{Logical; if TRUE delete the chr's cache before re-reading.}
}
\value{
Tibble with \verb{v_chr, v_pos, ref, alt, minor_allele, minor_AF, low_confidence_variant, beta, se, pval}.
}
\description{
Neale sumstats have ~13.8M rows and no chr/pos columns; the chr/pos info
is encoded in the single \code{variant = chr:pos:ref:alt} column. We must read
the whole file once to extract any chromosome's rows. This helper writes
just the requested chromosome's parsed slice to disk and returns it; later
calls for the same (file, chr) pair hit cache. A request for a different
chromosome of the same file pays the read cost again.
}
\keyword{internal}
