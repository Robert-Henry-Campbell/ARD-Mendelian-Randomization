% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess_exposure_snps.R
\name{.preprocess_defaults}
\alias{.preprocess_defaults}
\title{Apply the unified exposure-SNP preprocessing pipeline.}
\usage{
.preprocess_defaults()
}
\arguments{
\item{snps_tbl}{A TwoSampleMR-shaped tibble (must contain \code{SNP}).}

\item{clump_opts}{Named list overriding the defaults documented at
\code{?run_phenome_mr} (\code{p_backoff}, \code{r2}, \code{kb}, \code{f_threshold}, \code{maf_min},
\code{info_min}, \code{drop_indels}, \code{drop_palindromic}, \code{already_clumped},
\code{already_p_filtered}, \code{preprocess}). For backward compat,
\code{p_threshold} is accepted and translated to \code{p_backoff = c(p_threshold)} with a deprecation warning.}

\item{ancestry}{Ancestry code, e.g. "EUR".}

\item{cache_dir}{Cache root; LD reference is read from
\verb{<cache_dir>/ld_reference}.}

\item{confirm}{One of \code{"ask"}, \code{"yes"}, \code{"no"} -- passed to
\code{\link[=ld_reference_checker]{ld_reference_checker()}} when the LD panel must be installed for
clumping. \code{"no"} aborts cleanly if the panel is missing (no silent
download).}

\item{verbose}{Logical; per-step counts logged when TRUE.}
}
\value{
A list:
\itemize{
\item \code{snps}: filtered tibble (TwoSampleMR shape; may be 0-row).
\item \code{steps}: list of per-step accounting records (each has \code{step},
\code{n_in}, \code{n_out}, plus step-specific extras like \code{value},
\code{skipped_reason}).
\item \code{resolved_opts}: the merged \code{defaults <- modifyList(...) -> co}.
}
}
\description{
Steps, in order (locked):
\enumerate{
\item p-value filter        (default single rung c(5e-8); ladder opt-in;
skipped if \code{already_p_filtered = TRUE})
\item rsid validate + dedupe
\item drop indels           (default ON)
\item MAF filter            (default ON at \code{maf_min = 0.01}; pass
\code{maf_min = NULL} to disable; auto-skipped
when \code{eaf.exposure} is absent;
\code{pmin(eaf, 1-eaf)})
\item INFO filter           (default OFF; if \code{info_min} set AND an INFO
column is present; auto-detects
\code{SI}/\code{Rsq}/\code{R2}/\code{INFO}/\code{info})
\item palindromic drop      (default OFF; EAF-aware -- only ambiguous
palindromes (eaf in \link{0.42, 0.58}) dropped)
\item LD clumping           (always local against the ancestry-aware
1kg.v3 panel; skipped if
\code{already_clumped = TRUE})
\item F-statistic           (>= \code{f_threshold}; runs after clumping
because it's per-SNP and won't cause
locus loss)
}
}
\details{
Data-quality filters (3-6) run BEFORE clumping so the clump step
picks lead SNPs from a clean universe. F-stat runs AFTER clumping
because it's a per-SNP property (no locus loss).

Returns a list with the filtered tibble, per-step accounting, and the
merged \code{clump_opts}. A 0-row return is normal when filters strip
everything; the function never errors on empty output (caller decides).
}
\keyword{Canonical}
\keyword{Single}
\keyword{[.resolve_coloc_source()]}
\keyword{[preprocess_exposure_snps()]}
\keyword{and}
\keyword{by}
\keyword{cannot}
\keyword{defaults}
\keyword{drift.}
\keyword{for}
\keyword{internal}
\keyword{of}
\keyword{pipeline.}
\keyword{preprocess}
\keyword{shared}
\keyword{so}
\keyword{source}
\keyword{the}
\keyword{truth}
\keyword{two}
